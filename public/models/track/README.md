# Track kit GLBs

Asphalt-Comic wall, theme scenery, and on-track obstacle modules. Collision stays on the spline ribbon / obstacle radii (visuals may overhang).

| File | Use |
|------|-----|
| `tire-wall.glb` | Corner outer walls |
| `concrete-wall.glb` | Straight outer walls |
| `fence.glb` | Chain-link panel on top of concrete |
| `crane.glb` | Harbor gantry |
| `container.glb` | Harbor / Baustelle stacks (runtime tint) |
| `tank.glb` | Harbor silo |
| `grandstand.glb` | Parabolbogen yellow tribune |
| `palm.glb` / `hut.glb` | Beach props |
| `tower.glb` / `building.glb` | Schikanenring city |
| `cliff.glb` / `spire.glb` / `scrub.glb` | Omegatal canyon |
| `tree.glb` / `warehouse.glb` | Kuppenfinale forest + sheds |
| `ramp.glb` | Schanze / jump (`ramp` obstacles) |
| `rumble.glb` | Rüttelstreifen (`uneven` obstacles) |
| `oil.glb` | Ölspur (`oil` obstacles) |
| `tire-stack.glb` | Compact Reifenstapel (`tire_stack`) |
| `barrier.glb` | Compact Betonsperre (`concrete_barrier`) |

Rebuild: `npm run track:bake-tripo` (needs gitignored `assets/tripo-out/track/`).
See [SOURCES.md](./SOURCES.md).
